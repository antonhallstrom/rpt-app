/**
 * @fileOverview This is a simplistic createStore for a deeper understanding.
 * @author Anton Hallstrom
 * createStore
 * @param {function} reducer - A reducing function that return the next state tree
 * @param {*|Object} [preloadedState] - The initial state. But in most cases a plain object
 * @param {function} [enhancer] - The store enhancer. Third-party capabilitites such as middleware.
 */
