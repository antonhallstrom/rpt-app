/**
 * @fileOverview It decribes store enchancer of redux.
 * Store enchancers are Higher Order Functions, HOF.
 */

 /**
 * @fileOverview Higer Order Functions.
 *  HOC can take functions as parameters and return function as return values.
 *  A function that does either of those is called a higher order function.
 *
 * function x takes function y and returns y
 * @param {function} x - the first function
 * @param {function} y - the second function
 *
 * @returns {function} - returns function y
 */
// eslint-disable-next-line
const x = y => y

